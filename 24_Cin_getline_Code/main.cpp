#include<iostream>
#include<bits/stdc++.h>
using namespace std;

// As the name suggests, instead of reading a single letter by using cin.get()
// We can read entire sentence by cin.getline().  
// When we have to take large inputs and paragraphs use cin.getline().

int main() {
    
    // char sentence[1000];
    // cin.getline(sentence, 1000, '#');      // Sentence stops at #.
    // cout << sentence << endl;
    
    /*
    //Input:
     there is a
     paragraph
     but there # mine 

    // Output:
     there is a
     paragraph
     but there

    */
   // ======================================================================

// char paragraph[1000];

// cin.getline(paragraph, 1000, '.');
// cout << paragraph << endl;

   /*
   Input:
   this is the end of the line.
   Output:
   this is the end of the line
   */
    
   return 0;

}